using UnityEngine;
using System.Collections;

public class ChangeFootStepMaterial : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	/*
	void OnCollisionEnter(Collider coll)
	{
		if(coll.gameObject.tag =="Player")
		{
			footstepsController.AssignMaterial(GetComponent<Collider>().material);
			Debug.Log("changed");
		}
	}
*/
}
